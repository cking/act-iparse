image: node:latest

cache:
  paths:
  - node_modules/

before_script:
  - npm install

pages:
  script:
  - node script/build
  - mkdir public
  - mv build public/v2
  
  artifacts:
    paths:
    - public